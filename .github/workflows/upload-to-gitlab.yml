name: Upload PDF to GitLab

on:
  push:
    branches:
      - main

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Clone and modify from GitLab
        env:
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
        run: |
          git config --global user.name "Victor Garcia (GitHub Action)"
          git config --global user.email "victor.garcia@cimat.mx"

          # Clona el repositorio de GitLab
          git clone https://oauth2:${GITLAB_TOKEN}@gitlab.com/mackonde1/victorgarciads-site.git repo-gitlab
          echo "ya lo clone"

          # Elimina el PDF anterior
          rm -f repo-gitlab/assets/sources/Publications/Integral/Integración.pdf
          echo "borrado"

      - name: Commit and push removal
        run: |
          cd repo-gitlab
          git add assets/sources/Publications/Integral/Integración.pdf
          git commit -m "Removed old Integración.pdf from GitHub Action"
          git push origin main

      - name: Copy new PDF to GitLab repo
        run: |
          cp Integración.pdf repo-gitlab/assets/sources/Publications/Integral/
          echo "copiado nuevo PDF"

      - name: Commit and push new PDF
        run: |
          cd repo-gitlab
          git add assets/sources/Publications/Integral/Integración.pdf
          git commit -m "Uploaded new Integración.pdf from GitHub Action"
          git push origin main
